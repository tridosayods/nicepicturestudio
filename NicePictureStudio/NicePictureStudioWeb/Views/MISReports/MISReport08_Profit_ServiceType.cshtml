@model IEnumerable<NicePictureStudio.Models.BarChartViewModel>
<div class="chart-wrapper">
    @(Html.Kendo().Chart(Model)
            .Name("MISchart08")
            .Title("รายงานวิเคราะห์กาไรขั้นต้นเปรียบเทียบกับรายได้ตามแต่ละรูปแบบของการให้บริการ")
            .Legend(legend => legend
                .Visible(false)
            )
            .ChartArea(chartArea => chartArea
                .Background("transparent")
            )
            .Series(series =>
            {

                series.Column(
                    model => model.Data_Group1_1
                    ).Name("รายได้");
                series.Column(
                    model => model.Data_1
                    ).Name("กำไร");
            })
            .CategoryAxis(axis => axis
                .Categories(model => model.Catalogs)
                .MajorGridLines(lines => lines.Visible(false))
            )
            .ValueAxis(axis => axis
                .Numeric()
                .Max(15000000)
                .Line(line => line.Visible(false))
                .MajorGridLines(lines => lines.Visible(true))
                         .Labels(labels => labels.Format("{0:###,###} บาท"))
            )
            .Tooltip(tooltip => tooltip
                .Visible(true)
                        .Template("#= series.name #: #= kendo.toString(value,'\\#\\#,\\# บาท') #")
            )
    )
</div>


